import { ToolPageLayout } from "@/components/ToolPageLayout";
import { HandicapProtection } from "@/components/HandicapProtection";

const Protecao = () => {
  return (
    <ToolPageLayout title="Proteção de Handicap">
      <HandicapProtection />
    </ToolPageLayout>
  );
};

export default Protecao;
